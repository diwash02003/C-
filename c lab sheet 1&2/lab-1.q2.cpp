//2.WAP to multiply two numbers and display product.The Program should ask user for two numbers from keyboard.

#include<stdio.h>
int main(){
	int a,b,p;
	scanf("%d %d",&a,&b);
	p = a * b;
	printf("The product is %d.",p);
    return 0 ;
}
